# Railway Variables
SECRET_KEY=change-this-to-a-long-random-secret
ACCESS_TOKEN_EXPIRE_MINUTES=480

# SQLite: for persistence on Railway, attach a Volume and point DB_FILE at the mounted path.
# Example mount path: /data
DB_FILE=/data/kpi.db
