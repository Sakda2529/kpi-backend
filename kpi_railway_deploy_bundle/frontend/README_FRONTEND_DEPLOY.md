# Frontend Deploy Notes

You can host the frontend separately (for example GitHub Pages, Netlify, or Vercel).

In your HTML/JS, point `API_BASE` to the Railway backend domain and include the JWT token in `Authorization` headers.
